# CarStats

A data collection project written in python using `playwright` to collect data on car specifications.

It does this via webscrapping the reliable [https://caranddriver.com](https://caranddriver.com).

## Old Project

This project was previously written with `selenium` and is now sotred in the `archive` directory.